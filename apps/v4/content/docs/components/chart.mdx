---
title: Chart
description: Beautiful charts built using Recharts. Copy and paste into your apps.
component: true
---

Charts are built on top of [Recharts](https://recharts.org/en-US). The chart component provides a consistent API for building charts with built-in theming and accessibility.

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx pitsi@latest add chart
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the required dependencies:</Step>

```bash
npm install recharts
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="chart" title="components/ui/chart.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import {
  ChartContainer,
  ChartLegend,
  ChartLegendContent,
  ChartTooltip,
  ChartTooltipContent,
} from "@/components/ui/chart"
```

### Chart Config

The chart config defines the colors and labels for your chart data.

```tsx
const chartConfig = {
  desktop: {
    label: "Desktop",
    color: "hsl(var(--chart-1))",
  },
  mobile: {
    label: "Mobile",
    color: "hsl(var(--chart-2))",
  },
}
```

### Basic Example

```tsx
<ChartContainer config={chartConfig} className="h-[200px]">
  <BarChart data={data}>
    <Bar dataKey="desktop" fill="var(--color-desktop)" />
    <Bar dataKey="mobile" fill="var(--color-mobile)" />
    <ChartTooltip content={<ChartTooltipContent />} />
    <ChartLegend content={<ChartLegendContent />} />
  </BarChart>
</ChartContainer>
```

## Theming

Charts use CSS variables for theming. The default chart colors are:

- `--chart-1`: Primary chart color
- `--chart-2`: Secondary chart color
- `--chart-3` through `--chart-5`: Additional chart colors

You can customize these in your CSS:

```css
:root {
  --chart-1: 220 70% 50%;
  --chart-2: 160 60% 45%;
  --chart-3: 30 80% 55%;
  --chart-4: 280 65% 60%;
  --chart-5: 340 75% 55%;
}
```
